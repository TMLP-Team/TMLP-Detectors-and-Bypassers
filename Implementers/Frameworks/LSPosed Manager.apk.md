Please launch the LSPosed Manager via the root manager. No need to install the LSPosed Manager as an application under the ``/data/app`` via a ``.apk`` file. 

---

请通过 root 管理器或 LSPosed 寄生器打开 LSPosed 管理器，无需通过 ``.apk`` 文件将 LSPosed 管理器安装为位于 ``/data/app`` 下的应用程序。
